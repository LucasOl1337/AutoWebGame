# Swarm coordination

Shared coordination file for recurring Codex swarm sessions. Each session should claim a small, valuable, disjoint scope before editing and append evidence before closing.

## Active claims

| Session | Area | Scope | Files | Status |
|---------|------|-------|-------|--------|
| _none_ | - | - | - | - |

## Completed work

| Session | Before | After | Evidence | Commit |
|---------|--------|-------|----------|--------|
| i18n-storage-guard-20260707-1600 | Preferência de idioma lia e gravava `localStorage` diretamente, podendo quebrar a landing/troca de idioma quando storage do navegador estivesse bloqueado ou parcial. | Leitura de idioma agora cai para detecção normal quando storage falha, e persistência de idioma deixa de bloquear a experiência. | `npm run compile:esm`; `node tests/i18n-storage-guard-check.mjs`; `npm run build` | `b5f4dce` |
| bomb-explosion-sfx-variation | Bomb explosions always used the same `bomb_explode_default.mp3` clip, so repeated blasts sounded identical. | Explosion SFX now uses the existing default/main variants and avoids replaying the same variant back-to-back when alternatives exist. | `npm run compile:esm && node tests/sound-manager-variation-check.mjs`; `npm run build` | `04d5bff` |
| server-tick-monotonic-clock | Backward or invalid Worker clock samples could rewind the authoritative match tick clock and make the next pump over-catch-up. | The tick pump now clamps samples to a monotonic timestamp, preserving normal catch-up without bursts after clock regressions. | `npm run test:server-tick`; `npm run build` | `4e5f52c` |
| powerup-utility-drops | Shield, bomb-pass and kick existed as mechanics/test setup but never appeared in the deterministic crate drop pool. | Utility upgrades are typed, applied, prioritized by bots, shown in HUD state and generated as real crate drops. | `npm run test:powerup-drop-rate`; `npm run test:powerup-hud`; `npm run test:shield`; `npm run test:bomb-push`; `npm run build` | `9aa9dde` |
| bug-player-sprite-manifest-recovery | If the public character manifest request failed, the asset loader dropped to two default placeholder roster entries. | The loader now recovers from the approved TypeScript manifest, keeping Ranni, Killer Bee, Crocodilo Arcano and Nico selectable and sprite-loadable. | `npm run compile:esm`; `node tests/character-roster-manifest-fallback-check.mjs`; `npm run build`; `npm run test:roster-sync`; `npm run test:player-sprite` | `39d09be` |
| bot-target-live-opponents | Bots in 3/4-player matches could keep chasing defeated P1, and patrol could step into the only pathable tile even when it would explode shortly after arrival. | Bots now pick live active opponents, prefer vulnerable targets, and decline unsafe short-window patrol moves. | `npm run test:bot-target`; `npm run test:bot`; `npm run test:bot-remote`; `npm run test:bot-survival`; `npm run build` | `e707b41` |
| bot-spawn-protection-pressure | Bots could still chase a spawn-protected enemy, and the spawn-protection check used a stale protected-bot/solid-tile setup that no longer represented live gameplay. | Bots now chase only vulnerable enemies; while an enemy is protected they can reposition/open crates, and the check validates this from a playable tile after the bot's own protection expires. | `npm run compile:esm && node tests/bot-spawn-protection-check.mjs`; `node tests/bot-opening-discipline-check.mjs`; `node tests/bot-intel-check.mjs`; `node tests/bot-target-selection-check.mjs`; `node tests/bot-survival-10s-check.mjs`; `node tests/bot-powerup-priority-check.mjs`; `npm run build` | `d0abfcd` |
| fix-stuck-input-on-hide | Local controls could remain held if the tab became hidden before the browser delivered keyup, leaving movement/skill state latched after returning. | Hidden-tab visibility changes now clear held keys, queued presses and movement priority; input resumes normally after the tab becomes visible again. | `npm run test:input-visibility`; `npm run test:input`; `npm run test:input-alias`; `npm run test:online-input-latch`; `npm run build` | `2f3a344` |
| ux-local-controls-reference | The landing page offered bot match entry points but did not show the first local controls before play. | The landing copy now includes a compact local controls reference for movement, bomb pressure and ultimate use, localized in PT/EN and responsive on short/mobile screens. | `npm run build`; release critical test sweep | release integration |
| fix-lobby-actions-disconnected | Lobby setup actions could remain clickable during a WebSocket error and show optimistic feedback even though no message was sent. | Seat/ready actions require an open realtime socket and show reconnecting feedback while unavailable. | `node tests/lobby-disconnected-actions-check.mjs`; `node tests/lobby-rules-check.mjs`; `npm run build` | `73afdcc` |
| fix-arena-theme-tilepaths | Sprite arena themes resolved to invalid runtime URLs. | Sprite themes now use valid `/Assets/TileMaps/themes/...` paths and have a regression check. | `node tests/arena-theme-runtime-paths-check.mjs`; `npm run build` | `b5f68bf` |
| ux-session-return-brief | Landing forgot recent entry/result context after reload. | Landing renders a recent session brief and local match results persist through reload. | `node tests/session-return-brief-check.mjs`; `node tests/session-room-invite-link-check.mjs`; `npm run build` | `28a6255` |
| fix-room-code-normalization | Clipboard blocks and pasted invite URLs could break room sharing/joining. | Invite copy has fallback, and room code normalization extracts nested URL `room` values. | `node tests/session-room-invite-link-check.mjs`; `node tests/room-code-entry-normalization-check.mjs`; `npm run build` | `df825ae`, `65e4033` |
| ux-accessible-touch-focus | Several controls lacked visible focus or touch-friendly minimum size. | Key controls now have focus-visible styling and coarse-pointer target floors. | `node tests/touch-focus-css-check.mjs`; `npm run build` | `709e259` |
| fix-local-match-chrome | Local bot matches could show online-only chrome. | Local match hides invite/chat/info dock controls and returns cleanly to landing. | `node tests/local-match-chrome-check.mjs`; `npm run build` | `04e3285` |
| hud-critical-state-feedback | HUD did not call out imminent blast danger. | HUD shows `DANGER` for unprotected players in imminent blast tiles. | `node tests/hud-critical-state-feedback-check.mjs`; `node tests/danger-overlay-check.mjs`; `npm run build` | `826e476` |
| ux-match-end-next-actions | Round/match overlays did not clearly show score and next action. | Center overlay shows score, next round/match cue and local rematch/menu shortcuts. | `node tests/round-outcome-pause-check.mjs`; `node tests/match-result-shortcuts-check.mjs`; `npm run build` | `062e0ea`, `7cf7f26` |
| audio-sfx-stack-guard | Bomb placement and power-up collection could duplicate audio in the same frame. | Short SFX rate limits prevent same-frame stack while preserving quick recovery. | `node tests/sound-manager-variation-check.mjs`; `npm run build` | `88ca239` |
| ux-telemetry-storage-fallback | Blocked or partial storage could break telemetry/session client reads. | Telemetry and client storage reads/writes/removes are guarded. | `node tests/growth-telemetry-storage-check.mjs`; `node tests/local-match-chrome-check.mjs`; `npm run build` | `561c7be`, `72abcb4` |
| powerup-telegraph-polish | Power-up collection was easy to miss in HUD feedback. | Recently collected power-ups are highlighted in HUD slots and a short pickup notice. | `node tests/powerup-hud-slots-check.mjs`; `npm run build` | `5f7d433` |
| ux-round-start-cue | New rounds could restart without a clear visual "live" cue. | Canvas shows a short localized round-start cue on match/round transitions. | `node tests/round-start-cue-check.mjs`; `node tests/round-outcome-pause-check.mjs`; `npm run build` | `79138e7` |
| fix-input-page-scroll | Game keys could scroll the page when focus was outside inputs. | Game keydown/keyup prevent default scrolling while typing fields pass through. | `node tests/input-page-scroll-check.mjs`; `npm run build` | `8b4daf6` |
| ux-local-bot-intensity-selector | Landing always started local bot match with three bots. | Landing exposes and persists 1/2/3 bot intensity before local match start. | `node tests/menu-bot-fill-check.mjs`; `npm run build` | `ca34824` |
