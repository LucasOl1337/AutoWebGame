# Swarm coordination

Shared coordination file for recurring Codex swarm sessions. Each session should claim a small, valuable, disjoint scope before editing and append evidence before closing.

## Active claims

| Session | Area | Scope | Files | Status |
|---------|------|-------|-------|--------|
| _none_ | - | - | - | - |

## Completed work

| Session | Before | After | Evidence | Commit |
|---------|--------|-------|----------|--------|
| bomb-explosion-sfx-variation | Bomb explosions always used the same `bomb_explode_default.mp3` clip, so repeated blasts sounded identical. | Explosion SFX now uses the existing default/main variants and avoids replaying the same variant back-to-back when alternatives exist. | `npm run compile:esm && node tests/sound-manager-variation-check.mjs`; `npm run build` | `04d5bff` |
| server-tick-monotonic-clock | Backward or invalid Worker clock samples could rewind the authoritative match tick clock and make the next pump over-catch-up. | The tick pump now clamps samples to a monotonic timestamp, preserving normal catch-up without bursts after clock regressions. | `npm run test:server-tick`; `npm run build` | `4e5f52c` |
| powerup-utility-drops | Shield, bomb-pass and kick existed as mechanics/test setup but never appeared in the deterministic crate drop pool. | Utility upgrades are typed, applied, prioritized by bots, shown in HUD state and generated as real crate drops. | `npm run test:powerup-drop-rate`; `npm run test:powerup-hud`; `npm run test:shield`; `npm run test:bomb-push`; `npm run build` | `9aa9dde` |
| bug-player-sprite-manifest-recovery | If the public character manifest request failed, the asset loader dropped to two default placeholder roster entries. | The loader now recovers from the approved TypeScript manifest, keeping Ranni, Killer Bee, Crocodilo Arcano and Nico selectable and sprite-loadable. | `npm run compile:esm`; `node tests/character-roster-manifest-fallback-check.mjs`; `npm run build`; `npm run test:roster-sync`; `npm run test:player-sprite` | `39d09be` |
| bot-target-live-opponents | Bots in 3/4-player matches could keep chasing defeated P1, and patrol could step into the only pathable tile even when it would explode shortly after arrival. | Bots now pick live active opponents, prefer vulnerable targets, and decline unsafe short-window patrol moves. | `npm run test:bot-target`; `npm run test:bot`; `npm run test:bot-remote`; `npm run test:bot-survival`; `npm run build` | `e707b41` |
| bot-spawn-protection-pressure | Bots could still chase a spawn-protected enemy, and the spawn-protection check used a stale protected-bot/solid-tile setup that no longer represented live gameplay. | Bots now chase only vulnerable enemies; while an enemy is protected they can reposition/open crates, and the check validates this from a playable tile after the bot's own protection expires. | `npm run compile:esm && node tests/bot-spawn-protection-check.mjs`; `node tests/bot-opening-discipline-check.mjs`; `node tests/bot-intel-check.mjs`; `node tests/bot-target-selection-check.mjs`; `node tests/bot-survival-10s-check.mjs`; `node tests/bot-powerup-priority-check.mjs`; `npm run build` | `d0abfcd` |
| fix-stuck-input-on-hide | Local controls could remain held if the tab became hidden before the browser delivered keyup, leaving movement/skill state latched after returning. | Hidden-tab visibility changes now clear held keys, queued presses and movement priority; input resumes normally after the tab becomes visible again. | `npm run test:input-visibility`; `npm run test:input`; `npm run test:input-alias`; `npm run test:online-input-latch`; `npm run build` | `2f3a344` |
| ux-local-controls-reference | The landing page offered bot match entry points but did not show the first local controls before play. | The landing copy now includes a compact local controls reference for movement, bomb pressure and ultimate use, localized in PT/EN and responsive on short/mobile screens. | `npm run build`; release critical test sweep | release integration |
| fix-input-page-scroll | Gameplay keys could still interfere with browser/UI key handling: focused buttons/links lost Space/Enter activation, and keyup did not mirror scroll prevention. | Input now ignores typing and interactive controls while gameplay targets still prevent reserved key browser actions on keydown/keyup, keeping movement responsive without hijacking UI controls. | `npm run test:input-page-scroll`; `npm run test:input`; `npm run test:input-alias`; `npm run test:input-visibility`; `npm run build`; `codegraph impact InputManager`; `codegraph affected src/Engine/input.ts package.json tests/input-page-scroll-check.mjs` | `c7e65d6` |
